{
  "name": "project-2600-root",
  "version": "1.0.0",
  "description": "Orchestration scripts for MERN stack project.",
  "main": "server/server.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "install-all": "npm install && npm install --prefix client && npm install --prefix server",
    "build": "cd client && npm run build",
    "render-build":"npm run install-all && npm run build",
    "render-postbuild": "npm install --prefix client --include=dev && npm run build --prefix client"
  },
  "keywords": [
    "mern",
    "fullstack"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
